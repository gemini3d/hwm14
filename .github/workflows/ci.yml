name: ci

env:
  HOMEBREW_NO_INSTALL_CLEANUP: 1

on:
  push:
    paths:
      - "**.f"
      - "**.f90"
      - "**/CMakeLists.txt"
      - "**.cmake"
      - ".github/workflows/ci.yml"

jobs:

  unix:
    strategy:
      matrix:
        shared: [true, false]
        os: [ubuntu-latest, macos-latest]

    runs-on: ${{ matrix.os }}
    timeout-minutes: 15

    steps:
    - uses: actions/checkout@v3

    - name: Configure
      run: cmake -B build -DCMAKE_PREFIX_PATH=${{ runner.temp }} -DBUILD_SHARED_LIBS:BOOL=${{ matrix.shared }}

    - name: Build
      run: cmake --build build

    - name: Test
      run: ctest --test-dir build -V

    - name: Install
      run: cmake --install build

  windows:
    timeout-minutes: 20
    runs-on: windows-latest
    env:
      CMAKE_GENERATOR: MinGW Makefiles

        # shared always error 135 even on only shared. not a problem on real pc.

    steps:
    - uses: msys2/setup-msys2@v2
      with:
        update: true
        install: >-
          mingw-w64-x86_64-gcc-fortran

    - name: Put MSYS2_MinGW64 on PATH
      run: echo "${{ runner.temp }}/msys64/mingw64/bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

    - uses: actions/checkout@v2

    - name: Configure
      run: cmake -B build -DCMAKE_PREFIX_PATH=${{ runner.temp }}

    - name: Build
      run: cmake --build build

    - name: Test
      run: ctest --test-dir build -V

    - name: Install
      run: cmake --install build
